{
  "name": "bygeorgia-net",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "preview": "wrangler dev",
    "test": "node --test",
    "validate": "npm test && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
